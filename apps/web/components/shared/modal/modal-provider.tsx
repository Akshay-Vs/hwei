'use client';
import React, { Fragment } from 'react';
import StoreModal from './store-modal';

const ModalProvider = () => {
	return (
		<Fragment>
			<StoreModal />
		</Fragment>
	);
};

export default ModalProvider;
